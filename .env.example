# Server Configuration
PORT=5000
NODE_ENV="development"
BACKEND_URL="http://localhost:5000"
FRONTEND_URL="http://localhost:5173"

# Database Connection
DATABASE_URL="postgresql://neondb_owner:npg_DeJuhH0yWw3q@ep-aged-thunder-ahrj0nsr-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require"

# JWT Configuration
# ⚠️ IMPORTANT: Change this to a secure random string in production (min 32 chars)
JWT_SECRET="dev-secret-key-change-in-production-min-32-chars-long"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# Note: Each user configures their own Google OAuth credentials via:
# POST /api/auth/google/configure
# with their own Client ID, Client Secret, and Callback URL
